name: MVN build, SAST and SCA 

on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]

jobs:
 BuildWithGitHubActions:

   uses: ros132/Reusabletemplate/.github/workflows/reusable_template.yml@main
   
   with:
     file_path: pom.xml
